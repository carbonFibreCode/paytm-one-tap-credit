# n8n Cloud credentials. Copy to `n8n/.env.n8n` and fill in.
# This file is gitignored — nothing here is committed.
#
# Settings → n8n API → Create an API key

N8N_HOST_URL=https://your-instance.app.n8n.cloud
N8N_API_KEY=
